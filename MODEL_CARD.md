# Model Card: FoodArena Ranking Baseline v0.1.0

## 概述

本模型用于校园餐饮候选菜品排序。它不是大语言模型，而是五维线性效用模型。训练目标来自菜品两两偏好，模型输出效用分数；该分数只用于同一请求内排序，不是满意概率。

## 输入与输出

输入是用户预算、最高/偏好辣度、素食要求、过敏原、忌口和菜系偏好，以及最多 500 道菜。输出最多 20 个推荐、解释性特征贡献和所有被硬约束排除的菜。

硬约束在模型排序前执行，模型权重无法覆盖硬约束。浮点数须有限，未知字段会被拒绝，以降低拼写错误造成的静默失效。

## 训练数据

`v0.1.0` 使用代码生成的 1,200 个合成偏好对，固定随机种子为 42。合成标签由一组隐藏规则加随机噪声生成。这些数据不来自真实用户，也没有现实代表性。

## 当前评估

`reports/metrics.json` 记录对固定随机划分留出集的成对准确率和对数损失。这个测试仅用于发现训练管线回归。因为训练和验证偏好都来自同一合成生成器，它不能证明对真实学生、菜单或学校有效。

## 预期用途

- foodarena-ai 原型的候选菜品排序
- 数据契约和 API 联调
- 收集真实且经同意的偏好数据前的可解释基线

## 禁止用途与限制

- 不得作为食物过敏安全、营养或医疗建议
- 不得用于高风险决策、定价、资格审查或对人群进行不利区别对待
- 不应根据姓名、性别、民族、健康诊断等敏感属性排序
- 对新菜系、菜单信息错误、交叉接触、价格和库存变化没有可靠保证
- 模型可能偏向合成规则中的评分和偏好；不代表群体公平或长期满意度

## 上线前要求

用经同意的真实反馈重新训练；按用户和时间切分数据；报告约束违规率、无匹配率、NDCG/成对准确率、延迟以及不同预算和饮食偏好群体的覆盖率。设定回滚阈值，运行数据漂移监控，并让用户查看和修改其偏好。过敏原信息必须来自可信菜单源，并向用户展示核实提示。
